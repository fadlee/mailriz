{
  "name": "mailvault-cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "mailvault-cli": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --outdir dist --target node --format esm",
    "typecheck": "tsc --noEmit",
    "dev": "bun run src/cli.ts"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "@clack/prompts": "^0.8",
    "@mailvault/shared": "workspace:*",
    "picocolors": "^1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
