{
  "name": "rizmail-cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "rizmail-cli": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --outdir dist --target node --format esm --external wrangler",
    "typecheck": "tsc --noEmit",
    "dev": "bun run src/cli.ts"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "@clack/prompts": "^0.8",
    "@rizmail/shared": "workspace:*",
    "picocolors": "^1",
    "wrangler": "^4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
